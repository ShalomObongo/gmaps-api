---
phase: 05-reviews-collection-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/jobs-repo.ts
  - src/storage/jobs-repo.test.ts
  - src/api/schemas/job-input.ts
  - src/orchestration/intake/normalize-intake.ts
  - src/api/routes/jobs.ts
  - src/api/routes/jobs.test.ts
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "User can submit review collection controls (enabled, sort, maxReviews) with job intake and receives normalized accepted values in the API response."
    - "Invalid review sort values or out-of-range caps are rejected at intake instead of silently remapped."
    - "Worker-consumable review controls persist per job so each run has an explicit review extraction contract."
  artifacts:
    - path: "src/api/schemas/job-input.ts"
      provides: "Strict review controls schema with enum sort and bounded maxReviews"
      contains: "reviews"
    - path: "src/orchestration/intake/normalize-intake.ts"
      provides: "Normalized review controls defaults for all input variants"
      contains: "reviews"
    - path: "src/storage/jobs-repo.ts"
      provides: "Durable per-job review config persistence and hydration"
      contains: "reviewConfigJson"
  key_links:
    - from: "src/api/routes/jobs.ts"
      to: "src/orchestration/intake/normalize-intake.ts"
      via: "POST /jobs parses and normalizes review controls before persistence"
      pattern: "normalizeIntakeInput|reviews"
    - from: "src/api/routes/jobs.ts"
      to: "src/storage/jobs-repo.ts"
      via: "normalized review controls are serialized onto queued jobs"
      pattern: "jobsRepo.create|reviewConfigJson"
---

<objective>
Establish the review control contract at API intake and persist resolved controls on queued jobs so review extraction is explicit, validated, and deterministic per run.

Purpose: DATA-03 requires user-controlled sort and cap behavior, which must be locked in before worker extraction starts.
Output: Intake schema and normalization updates for reviews plus migration-safe job persistence of resolved review controls.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-reviews-collection-controls/05-RESEARCH.md
@.planning/phases/03-coverage-and-deduplication/03-coverage-and-deduplication-01-SUMMARY.md
@src/api/schemas/job-input.ts
@src/orchestration/intake/normalize-intake.ts
@src/api/routes/jobs.ts
@src/storage/jobs-repo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration-safe job persistence for resolved review controls</name>
  <files>src/storage/schema.ts, src/storage/db.ts, src/storage/jobs-repo.ts, src/storage/jobs-repo.test.ts</files>
  <action>Add a typed review control config contract to storage (`enabled`, `sort`, `maxReviews`) and persist it per job as JSON (`review_config_json`) with additive `ensureColumn` migration safety. Extend repo create/select mappings so queued jobs always hydrate one deterministic review config payload. Keep terminal-state lifecycle semantics unchanged; do not alter claim/update/complete/fail transition rules.</action>
  <verify>
    <automated>npm run test -- src/storage/jobs-repo.test.ts -x</automated>
    <manual>Optional: create two jobs with different review configs and confirm each record preserves its own config without leakage.</manual>
    <sampling_rate>run after repository migration and mapping updates</sampling_rate>
  </verify>
  <done>Job records durably store and return review controls with additive DB upgrade behavior for existing local databases.</done>
</task>

<task type="auto">
  <name>Task 2: Extend POST /jobs contract with strict review controls and normalized defaults</name>
  <files>src/api/schemas/job-input.ts, src/orchestration/intake/normalize-intake.ts, src/api/routes/jobs.ts, src/api/routes/jobs.test.ts</files>
  <action>Extend intake schema to accept optional `reviews` controls with strict enum sort values (`newest`, `most_relevant`, `highest_rating`, `lowest_rating`) and bounded `maxReviews` limit, then normalize to an internal shape for every accepted job (`enabled=false`, `sort='newest'`, `maxReviews=0` defaults when omitted). Echo normalized review controls in the accepted response and serialize them into `jobsRepo.create` via `reviewConfigJson`. Reject unknown sort values and out-of-range caps at the API boundary; do not silently coerce invalid values.</action>
  <verify>
    <automated>npm run test -- src/api/routes/jobs.test.ts -x</automated>
    <manual>POST /jobs with reviews enabled and custom sort/cap; response input section should return the normalized reviews object and invalid sorts should 400.</manual>
    <sampling_rate>run after intake schema, route wiring, and route tests are updated</sampling_rate>
  </verify>
  <done>Users can submit explicit review controls, receive normalized accepted values, and have those controls persisted for worker execution.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/storage/jobs-repo.test.ts src/api/routes/jobs.test.ts -x` passes.
- Existing jobs lifecycle behavior remains intact while review controls are persisted per job.
- Invalid review sort/cap values are rejected at intake, and accepted jobs include normalized review controls in response payload.
</verification>

<success_criteria>
- Every accepted job has deterministic review controls available to the worker (`enabled`, `sort`, `maxReviews`).
- API input validation enforces only supported review sort values and bounded review caps.
- Per-job review control persistence works across new and pre-existing SQLite files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reviews-collection-controls/05-reviews-collection-controls-01-SUMMARY.md`
</output>
