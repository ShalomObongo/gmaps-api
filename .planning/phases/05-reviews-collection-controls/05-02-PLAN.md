---
phase: 05-reviews-collection-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/crawler/maps/extract-place-reviews.ts
  - src/crawler/maps/extract-place-reviews.test.ts
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/place-reviews-repo.ts
  - src/storage/place-reviews-repo.test.ts
  - src/orchestration/runner/jobs-worker.ts
  - src/orchestration/runner/jobs-worker.test.ts
  - src/server.ts
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "When review extraction is enabled, run output includes persisted review rows tied to each place."
    - "Review ordering in persisted output matches the requested sort selection for the run."
    - "Collected reviews per place never exceed the configured maxReviews cap."
  artifacts:
    - path: "src/crawler/maps/extract-place-reviews.ts"
      provides: "Sort-aware, cap-enforced review extraction contract returning deterministic sequence order"
      exports: ["extractPlaceReviews"]
    - path: "src/storage/place-reviews-repo.ts"
      provides: "Per-place review row persistence with unique review identity and ordered position"
      exports: ["createPlaceReviewsRepo"]
    - path: "src/orchestration/runner/jobs-worker.ts"
      provides: "Worker wiring from review config to extractor and persisted review rows"
      exports: ["createJobsWorker"]
  key_links:
    - from: "src/orchestration/runner/jobs-worker.ts"
      to: "src/crawler/maps/extract-place-reviews.ts"
      via: "worker invokes review extraction only when review controls are enabled"
      pattern: "reviews|extractPlaceReviews|maxReviews|sort"
    - from: "src/orchestration/runner/jobs-worker.ts"
      to: "src/storage/place-reviews-repo.ts"
      via: "worker persists ordered review rows with cap-respecting sequence"
      pattern: "placeReviewsRepo|insertMany|position|sortOrder"
---

<objective>
Implement the review extraction data path so enabled runs persist ordered, capped review records tied to places and ready for downstream results APIs.

Purpose: DATA-03 is only met when sort and cap controls materially shape collected review output, not just intake payloads.
Output: Review extractor module, durable `place_reviews` persistence, and worker wiring that enforces requested ordering and cap during ingestion.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-reviews-collection-controls/05-RESEARCH.md
@.planning/phases/04-place-detail-extraction/04-place-detail-extraction-02-SUMMARY.md
@.planning/phases/05-reviews-collection-controls/05-01-PLAN.md
@src/orchestration/runner/jobs-worker.ts
@src/crawler/maps/extract-place-details.ts
@src/storage/places-repo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sort-aware review extraction module with hard-cap enforcement</name>
  <files>src/crawler/maps/extract-place-reviews.ts, src/crawler/maps/extract-place-reviews.test.ts</files>
  <action>Create a dedicated review extraction module that accepts resolved controls (`sort`, `maxReviews`) and returns normalized review candidates in source order with explicit `position` sequencing. Implement deterministic sort-option mapping (`newest`, `most_relevant`, `highest_rating`, `lowest_rating`) and stop appending immediately when `maxReviews` is reached (no batch overrun). Keep extractor resilient to selector misses by returning partial/nullable review fields instead of throwing run-level failures.</action>
  <verify>
    <automated>npm run test -- src/crawler/maps/extract-place-reviews.test.ts -x</automated>
    <manual>Optional: run extractor against a fixture with repeated cards and confirm returned review IDs remain unique, ordered, and capped.</manual>
    <sampling_rate>run after extractor and cap/order tests are implemented</sampling_rate>
  </verify>
  <done>Extractor produces normalized ordered review candidates matching requested sort and never exceeding configured cap.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration-safe place_reviews storage and ordered review repository</name>
  <files>src/storage/schema.ts, src/storage/db.ts, src/storage/place-reviews-repo.ts, src/storage/place-reviews-repo.test.ts</files>
  <action>Add typed review record contracts and create a normalized `place_reviews` table with `job_id`, `place_key`, `review_id`, `sort_order`, `position`, and nullable review detail fields. Enforce retry-safe dedup via `UNIQUE(job_id, place_key, review_id)` with conflict no-op inserts, and expose repository methods to persist/list ordered reviews by job and place. Keep DB changes additive and startup-safe for existing local databases.</action>
  <verify>
    <automated>npm run test -- src/storage/place-reviews-repo.test.ts -x</automated>
    <manual>Insert duplicate review IDs for the same job/place and verify one row persists while ordering by position remains stable.</manual>
    <sampling_rate>run after schema, migration, and repository wiring</sampling_rate>
  </verify>
  <done>Review rows persist durably with deterministic order and dedup guarantees needed for future API/export retrieval.</done>
</task>

<task type="auto">
  <name>Task 3: Wire worker ingestion to apply review controls and persist ordered capped reviews</name>
  <files>src/orchestration/runner/jobs-worker.ts, src/orchestration/runner/jobs-worker.test.ts, src/server.ts</files>
  <action>Extend worker dependencies to include the review repository and review extractor seam, parse persisted `reviewConfigJson` per job, and run extraction only when `enabled=true` with `maxReviews>0`. Persist extracted rows with `sortOrder` and `position` for each accepted place and maintain existing run reliability semantics (review extraction failures should not crash whole runs; fallback to empty review set per place). Keep discovered/processed metrics behavior unchanged unless explicitly adding review counters with tests.</action>
  <verify>
    <automated>npm run test -- src/orchestration/runner/jobs-worker.test.ts -x</automated>
    <manual>Execute a worker scenario with reviews enabled and confirm persisted review rows respect requested sort and cap, while jobs still complete successfully.</manual>
    <sampling_rate>run after worker/repo/server integration is complete</sampling_rate>
  </verify>
  <done>Enabled jobs persist review output that follows requested ordering and cap without regressing worker completion behavior.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/crawler/maps/extract-place-reviews.test.ts src/storage/place-reviews-repo.test.ts src/orchestration/runner/jobs-worker.test.ts -x` passes.
- Worker writes review rows only for review-enabled jobs and preserves deduped place ingestion behavior.
- Stored review rows are ordered by persisted `position` and never exceed configured `maxReviews` per place.
</verification>

<success_criteria>
- DATA-03 output path exists end-to-end: controls -> extraction -> ordered/capped persistence.
- Requested review sort is represented in persisted rows and reflected by retrieval ordering.
- Review cap is enforced as a hard maximum during extraction and storage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reviews-collection-controls/05-reviews-collection-controls-02-SUMMARY.md`
</output>
