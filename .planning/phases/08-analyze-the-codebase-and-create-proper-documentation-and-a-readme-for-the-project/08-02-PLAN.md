---
phase: 08-analyze-the-codebase-and-create-proper-documentation-and-a-readme-for-the-project
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api.md
  - docs/architecture.md
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "A user can understand request/response behavior for every public endpoint without reading route source code."
    - "A user can see state-gated API semantics (404, 409, 200/202) and avoid misinterpreting in-progress job responses."
    - "A contributor can follow the runtime data flow from intake to worker persistence to results/export output assembly."
  artifacts:
    - path: "docs/api.md"
      provides: "Endpoint reference with payload examples and status semantics derived from route schemas and tests"
      contains: "/jobs/:id/exports"
    - path: "docs/architecture.md"
      provides: "Module-level architecture and data flow across API, orchestration, crawler, storage, and output layers"
      contains: "buildJobResultsModel"
    - path: "src/api/routes/job-results.ts"
      provides: "Canonical state-gated results contract used as source for docs"
      exports: ["registerJobResultsRoutes"]
  key_links:
    - from: "docs/api.md"
      to: "src/api/routes/job-results.ts"
      via: "documented 404/409/200 state gates and response model"
      pattern: "results_not_ready|/jobs/:id/results"
    - from: "docs/api.md"
      to: "src/api/routes/job-exports.ts"
      via: "documented export format branching and attachment behavior"
      pattern: "format=json\|csv|content-disposition"
    - from: "docs/architecture.md"
      to: "src/server.ts"
      via: "described server composition and route/worker wiring"
      pattern: "registerJobRoutes|createJobsWorker|registerJobExportsRoutes"
---

<objective>
Document the public API contract and internal architecture using code-verified source-of-truth mapping.

Purpose: Phase 8 requires proper project documentation, and the highest-risk drift area is API behavior and runtime wiring; these docs make those contracts explicit and auditable.
Output: `docs/api.md` and `docs/architecture.md` reflecting current route schemas, repository/query behavior, and worker/output module connections.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-analyze-the-codebase-and-create-proper-documentation-and-a-readme-for-the-project/08-RESEARCH.md
@src/server.ts
@src/api/routes/jobs.ts
@src/api/routes/job-status.ts
@src/api/routes/job-results.ts
@src/api/routes/job-exports.ts
@src/output/build-job-results-model.ts
@src/storage/jobs-repo.ts
@src/api/routes/jobs.test.ts
@src/api/routes/job-results.test.ts
@src/api/routes/job-exports.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build API contract reference from route schemas and tests</name>
  <files>docs/api.md</files>
  <action>Create `docs/api.md` covering `/health`, `POST /jobs`, `GET /jobs/:id`, `GET /jobs/:id/results`, and `GET /jobs/:id/exports`. For each endpoint, include purpose, required inputs, key response fields, and explicit status/error semantics. Source request/response details from route schemas and matching tests (especially 404/409 state gates and export `format` behavior). Include copy-pastable curl examples that use current payload shapes from tests; do not document unsupported URL shapes or nonexistent response fields.</action>
  <verify>
    <automated>test -f docs/api.md && npm run test -- src/api/routes/jobs.test.ts src/api/routes/job-status.test.ts src/api/routes/job-results.test.ts src/api/routes/job-exports.test.ts -x</automated>
    <manual>Optionally inspect docs/api.md and confirm each endpoint section cites real behavior present in route handlers/tests.</manual>
    <sampling_rate>run after API reference draft is complete</sampling_rate>
  </verify>
  <done>API documentation reflects the current implemented contract, including ready vs not-ready state behavior and export format semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Build architecture overview with module boundaries and data flow</name>
  <files>docs/architecture.md</files>
  <action>Create `docs/architecture.md` that maps runtime composition in `src/server.ts`, describes responsibilities of config/orchestration/crawler/storage/output modules, and explains end-to-end flow: intake normalization -> queued persistence -> worker execution -> place/review persistence -> results/export read models. Include where guardrails and rate limiting are enforced, and where deterministic output assembly happens via `buildJobResultsModel`. Keep diagrams/text grounded in actual file boundaries and avoid speculative future architecture.</action>
  <verify>
    <automated>test -f docs/architecture.md && npm run test -- src/orchestration/runner/jobs-worker.test.ts src/output/build-job-results-model.test.ts src/server.test.ts -x</automated>
    <manual>Optionally cross-check architecture sections against referenced source files for naming and wiring accuracy.</manual>
    <sampling_rate>run after architecture doc is drafted</sampling_rate>
  </verify>
  <done>Architecture document gives contributors an accurate mental model of runtime module connections and data movement through the system.</done>
</task>

</tasks>

<verification>
- `docs/api.md` and `docs/architecture.md` exist with source-anchored endpoint and architecture content.
- API docs preserve implemented error/status semantics for not-found and not-ready states.
- Architecture docs reflect current server wiring and output assembly modules.
</verification>

<success_criteria>
- DOCS-03 is satisfied: users can understand and use the API contract and contributors can understand system flow from documentation alone.
- Endpoint examples and status semantics match tested behavior in route test suites.
- No invented modules, commands, or response fields appear in docs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-analyze-the-codebase-and-create-proper-documentation-and-a-readme-for-the-project/08-analyze-the-codebase-and-create-proper-documentation-and-a-readme-for-the-project-02-SUMMARY.md`
</output>
