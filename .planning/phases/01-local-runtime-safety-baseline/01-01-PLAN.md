---
phase: 01-local-runtime-safety-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/config/env.ts
  - src/config/runtime-defaults.ts
  - src/storage/db.ts
  - src/storage/schema.ts
  - src/storage/jobs-repo.ts
  - src/orchestration/policy.ts
  - src/api/routes/jobs.ts
  - src/server.ts
autonomous: true
requirements:
  - RELY-02
must_haves:
  truths:
    - "User can start the API locally without configuring paid proxy or captcha services."
    - "User can submit a local job and receive queued acceptance with effective runtime policy."
  artifacts:
    - path: "package.json"
      provides: "Local runtime dependency baseline using Fastify, Crawlee, Playwright, SQLite stack"
      contains: "fastify"
    - path: "src/config/runtime-defaults.ts"
      provides: "Paid-service-free default runtime policy values"
      exports: ["RUNTIME_DEFAULTS", "buildRuntimePolicy"]
    - path: "src/api/routes/jobs.ts"
      provides: "POST /jobs endpoint that accepts local job input and returns 202"
      exports: ["registerJobRoutes"]
    - path: "src/storage/jobs-repo.ts"
      provides: "SQLite-backed job persistence for local execution"
      exports: ["createJobsRepo"]
  key_links:
    - from: "src/api/routes/jobs.ts"
      to: "src/orchestration/policy.ts"
      via: "job input normalization to effective policy"
      pattern: "buildRuntimePolicy|resolveJobPolicy"
    - from: "src/api/routes/jobs.ts"
      to: "src/storage/jobs-repo.ts"
      via: "persist queued job before enqueue"
      pattern: "jobsRepo\\.(create|insert)"
---

<objective>
Create the local runtime foundation so the API can run and accept jobs with no mandatory paid dependencies.

Purpose: Deliver RELY-02 first so later reliability and safety controls are layered on a runnable local baseline.
Output: Bootstrapped TypeScript service, local storage/repository layer, and queued job intake route with effective policy echo.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-safety-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap local runtime and default config surface</name>
  <files>package.json, src/config/env.ts, src/config/runtime-defaults.ts, src/server.ts</files>
  <action>Initialize a Node 22 + TypeScript service with Fastify, Crawlee, Playwright, Drizzle, and better-sqlite3. Add validated env/config loading that keeps proxy and captcha integrations optional/off by default, define conservative local runtime defaults, and wire Fastify startup with a visible startup banner that states local-only baseline behavior. Do not add mandatory external service credentials or hard dependencies on paid providers.</action>
  <verify>
    <automated>npm run build && npm run start:check</automated>
    <manual>Confirm startup logs include guardrail baseline note and no required proxy/captcha env vars.</manual>
    <sampling_rate>run once after task completion</sampling_rate>
  </verify>
  <done>Service compiles and starts locally; effective defaults are centralized and no paid service is required to boot.</done>
</task>

<task type="auto">
  <name>Task 2: Implement local job intake with persisted queue-ready records</name>
  <files>src/storage/db.ts, src/storage/schema.ts, src/storage/jobs-repo.ts, src/orchestration/policy.ts, src/api/routes/jobs.ts</files>
  <action>Create SQLite schema/repository for jobs, implement policy normalization that merges user input with safe defaults, and add POST /jobs that persists queued jobs and returns 202 with jobId, status, and resolved policy. Keep execution async (no in-request scrape), and ensure endpoint response explicitly reflects local baseline configuration so users can inspect applied defaults.</action>
  <verify>
    <automated>npm run test -- src/api/routes/jobs.test.ts -x</automated>
    <manual>POST a minimal job payload and verify response includes queued status and effective policy fields.</manual>
    <sampling_rate>run after route and repository wiring</sampling_rate>
  </verify>
  <done>Jobs can be submitted and persisted locally with explicit resolved runtime policy, without triggering synchronous scrape execution.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- `npm run test -- src/api/routes/jobs.test.ts -x` passes with local SQLite setup.
- `curl -X POST http://localhost:3000/jobs` returns `202` and includes `jobId`, `status`, and `policy` fields.
</verification>

<success_criteria>
- Local API starts on a clean machine with only documented Node/npm prerequisites.
- No paid proxy/captcha service is required for startup or job submission.
- Job intake persists queued records and returns visible applied defaults in response payload.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-safety-baseline/01-local-runtime-safety-baseline-01-SUMMARY.md`
</output>
