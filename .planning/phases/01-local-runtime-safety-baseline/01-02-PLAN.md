---
phase: 01-local-runtime-safety-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/orchestration/backoff.ts
  - src/orchestration/pacing.ts
  - src/crawler/runner.ts
  - src/api/plugins/rate-limit.ts
  - src/api/routes/jobs.ts
  - src/safety/guardrails.ts
  - src/safety/sensitive-fields.ts
  - src/api/schemas/job-input.ts
  - src/server.ts
autonomous: true
requirements:
  - RELY-01
  - SAFE-01
must_haves:
  truths:
    - "User-submitted jobs automatically run with retry, backoff, and pacing defaults."
    - "User sees explicit responsible-use guardrails before and during usage."
    - "Sensitive field collection is opt-in and disabled by default."
  artifacts:
    - path: "src/orchestration/backoff.ts"
      provides: "Deterministic exponential backoff with jitter and max cap"
      exports: ["computeBackoffMs", "classifyRetryableError"]
    - path: "src/api/plugins/rate-limit.ts"
      provides: "Global and route-level API rate limits"
      exports: ["registerRateLimitPlugin"]
    - path: "src/safety/sensitive-fields.ts"
      provides: "Explicit sensitive field gate and default-off policy"
      exports: ["resolveSensitiveFieldPolicy", "DEFAULT_SAFE_FIELDS"]
    - path: "src/safety/guardrails.ts"
      provides: "Responsible-use notices and runtime warning generation"
      exports: ["GUARDRAIL_NOTICE", "buildRunWarnings"]
  key_links:
    - from: "src/api/routes/jobs.ts"
      to: "src/safety/sensitive-fields.ts"
      via: "input validation and effective policy resolution"
      pattern: "includeSensitiveFields|resolveSensitiveFieldPolicy"
    - from: "src/crawler/runner.ts"
      to: "src/orchestration/backoff.ts"
      via: "retry delay calculation on failures"
      pattern: "computeBackoffMs"
    - from: "src/api/plugins/rate-limit.ts"
      to: "src/server.ts"
      via: "plugin registration during app boot"
      pattern: "registerRateLimitPlugin"
---

<objective>
Add reliability and safety enforcement so each run uses conservative retry/backoff/pacing behavior and clear guardrails by default.

Purpose: Satisfy RELY-01 and SAFE-01 with code-level enforcement, not documentation-only guidance.
Output: Retry/backoff/pacing policy wiring, guardrail and sensitive-field controls, and app-boot enforcement of API limits.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-safety-baseline/01-RESEARCH.md
@.planning/phases/01-local-runtime-safety-baseline/01-local-runtime-safety-baseline-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce retry, backoff, and pacing defaults in crawler runtime</name>
  <files>src/orchestration/backoff.ts, src/orchestration/pacing.ts, src/crawler/runner.ts</files>
  <action>Implement failure classification for transient/block-like errors, capped exponential backoff with jitter, and inter-action pacing delays from policy defaults. Wire these controls into crawler execution so all jobs inherit defaults automatically unless explicitly overridden within safe bounds. Emit structured warning signals (`BLOCK_DETECTED`, `HIGH_RETRY_RATE`) for degraded runs directly from runner/backoff flow (no standalone signal shim file).</action>
  <verify>
    <automated>npm run test -- src/orchestration/backoff.test.ts src/crawler/runner.test.ts -x</automated>
    <manual>Run a simulated flaky target and confirm retries/backoff events are logged and capped.</manual>
    <sampling_rate>run after runtime policy wiring</sampling_rate>
  </verify>
  <done>Runtime applies retry/backoff/pacing defaults on failures and surfaces machine-readable degraded-state warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Add enforceable guardrails, opt-in sensitive fields, and usage messaging</name>
  <files>src/api/plugins/rate-limit.ts, src/api/routes/jobs.ts, src/api/schemas/job-input.ts, src/safety/guardrails.ts, src/safety/sensitive-fields.ts, src/server.ts</files>
  <action>Register global API rate limits with stricter `/jobs` limits, validate job input so sensitive fields require explicit opt-in, and default to safe field allowlist when opt-in is absent. Explicitly register `registerRateLimitPlugin` during Fastify app boot in `src/server.ts` before route registration so limits are active for all requests. Expose responsible-use notices in startup/runtime responses and reject unsafe request combinations instead of silently accepting them.</action>
  <verify>
    <automated>npm run test -- src/api/routes/jobs.guardrails.test.ts src/server.test.ts -x</automated>
    <manual>Submit one default request and one sensitive opt-in request, then confirm payload differences and guardrail warnings.</manual>
    <sampling_rate>run after API safety controls are wired</sampling_rate>
  </verify>
  <done>Rate limits are active via server boot registration, sensitive fields are default-off with explicit opt-in, and guardrail notices are visible in runtime surfaces.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/orchestration/backoff.test.ts src/crawler/runner.test.ts -x` passes.
- `npm run test -- src/api/routes/jobs.guardrails.test.ts src/server.test.ts -x` passes.
- Manual API smoke confirms default request excludes sensitive fields and opt-in request includes them only when allowed.
</verification>

<success_criteria>
- Every queued job uses conservative retry/backoff/pacing defaults unless explicitly overridden within validated bounds.
- Guardrails are enforced at API boundaries (rate limiting + invalid unsafe combos rejected).
- Sensitive field collection is off by default and only enabled via explicit input flag.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-safety-baseline/01-local-runtime-safety-baseline-02-SUMMARY.md`
</output>
