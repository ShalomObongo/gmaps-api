---
phase: 02-job-intake-and-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/jobs-repo.ts
  - src/orchestration/runner/jobs-worker.ts
  - src/api/routes/job-status.ts
  - src/server.ts
  - src/storage/jobs-repo.test.ts
  - src/api/routes/job-status.test.ts
autonomous: true
requirements:
  - JOBS-01
  - JOBS-02
must_haves:
  truths:
    - "User can observe job lifecycle states moving through queued, running, completed, or failed."
    - "User can fetch live progress counters and heartbeat timestamps while a job executes."
    - "User can poll a status endpoint and see durable run metrics instead of transient in-memory-only values."
  artifacts:
    - path: "src/storage/db.ts"
      provides: "SQLite schema with lifecycle timestamps and progress counters for job visibility"
      contains: "ALTER/CREATE fields for started_at, finished_at, heartbeat and counters"
    - path: "src/storage/jobs-repo.ts"
      provides: "Transactional lifecycle/progress read-model updates and status retrieval"
      exports: ["createJobsRepo"]
    - path: "src/api/routes/job-status.ts"
      provides: "GET /jobs/:id visibility endpoint returning status, progress, and basic metrics"
      exports: ["registerJobStatusRoutes"]
    - path: "src/orchestration/runner/jobs-worker.ts"
      provides: "Background queued-job worker that claims jobs and writes running/completed/failed transitions"
      exports: ["createJobsWorker"]
  key_links:
    - from: "src/orchestration/runner/jobs-worker.ts"
      to: "src/storage/jobs-repo.ts"
      via: "status transition and heartbeat/progress updates"
      pattern: "markRunning|markCompleted|markFailed|updateProgress"
    - from: "src/api/routes/job-status.ts"
      to: "src/storage/jobs-repo.ts"
      via: "read-model fetch for polling responses"
      pattern: "getById|getProgressSnapshot"
    - from: "src/server.ts"
      to: "src/orchestration/runner/jobs-worker.ts"
      via: "worker lifecycle start/stop hooks"
      pattern: "createJobsWorker|start|stop"
---

<objective>
Implement durable lifecycle and progress visibility so jobs transition through v1 statuses and expose live execution metrics via polling endpoints.

Purpose: Satisfy JOBS-01 and JOBS-02 with persisted state transitions and near-real-time API visibility.
Output: SQLite-backed lifecycle/progress model, background worker transition wiring, and `GET /jobs/:id` status endpoint with tests.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-job-intake-and-visibility/02-RESEARCH.md
@.planning/phases/01-local-runtime-safety-baseline/01-local-runtime-safety-baseline-02-SUMMARY.md
@src/storage/db.ts
@src/storage/jobs-repo.ts
@src/storage/schema.ts
@src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SQLite job read model for lifecycle timestamps and live counters</name>
  <files>src/storage/schema.ts, src/storage/db.ts, src/storage/jobs-repo.ts, src/storage/jobs-repo.test.ts</files>
  <action>Expand job persistence to store lifecycle timestamps (`startedAt`, `finishedAt`, `failedAt`, `lastHeartbeatAt`) plus progress counters (`discoveredCount`, `processedCount`, `failedCount`) and an optional failure reason. Add repo methods for transactional state transitions (`queued -> running -> completed|failed`), periodic heartbeat/progress updates, and read-model retrieval that powers polling APIs. Keep terminal state immutable (never transition out of completed/failed), and ensure transition + metric writes that must stay consistent run in one SQLite transaction. Add focused repository tests proving valid transitions, invalid transition rejection, and persisted progress updates.</action>
  <verify>
    <automated>npm run test -- src/storage/jobs-repo.test.ts -x</automated>
    <manual>Optional: inspect local SQLite row values after simulated transitions to confirm timestamps/counters persist.</manual>
    <sampling_rate>run after lifecycle repo APIs and tests are implemented</sampling_rate>
  </verify>
  <done>SQLite read model can persist lifecycle transitions and progress counters with transactionally consistent state.</done>
</task>

<task type="auto">
  <name>Task 2: Add background worker loop to claim queued jobs and update lifecycle/progress</name>
  <files>src/orchestration/runner/jobs-worker.ts, src/storage/jobs-repo.ts, src/server.ts</files>
  <action>Create a lightweight background worker that periodically claims the next queued job, marks it running, emits heartbeat/progress updates during execution, and finishes with completed or failed status. Wire worker startup in server boot and clean shutdown in `onClose` so lifecycle updates occur during normal API runtime, not request threads. Use existing runner/retry policy utilities for execution wrappers where possible, and avoid scraping work in `POST /jobs` handlers. Keep worker polling interval bounded and configurable through runtime defaults/environment so local runs remain predictable.</action>
  <verify>
    <automated>npm run test -- src/storage/jobs-repo.test.ts src/server.test.ts -x</automated>
    <manual>Start server, submit one job, and poll until status changes from queued to running to a terminal state.</manual>
    <sampling_rate>run after worker boot/shutdown wiring</sampling_rate>
  </verify>
  <done>Queued jobs transition through lifecycle states automatically while the service is running, with persisted heartbeat/progress data.</done>
</task>

<task type="auto">
  <name>Task 3: Expose polling visibility endpoint for lifecycle, progress, and basic metrics</name>
  <files>src/api/routes/job-status.ts, src/server.ts, src/api/routes/job-status.test.ts</files>
  <action>Add `GET /jobs/:id` endpoint that returns job id, lifecycle status, lifecycle timestamps, progress counters, and basic metrics such as elapsed milliseconds and last heartbeat age. Return `404` for unknown IDs and preserve machine-readable error shape. Register the endpoint during server boot and add integration tests covering queued/running/terminal snapshots and missing-job behavior. Keep transport polling-first (no SSE in this phase) and ensure response values come from durable repo read model, not in-memory worker state.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-status.test.ts src/api/routes/jobs.test.ts -x</automated>
    <manual>Poll `/jobs/:id` every 1-2 seconds during a run and confirm status/progress fields update over time.</manual>
    <sampling_rate>run after endpoint registration and tests</sampling_rate>
  </verify>
  <done>User can poll job status and see real lifecycle/progress updates sourced from persisted state.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/storage/jobs-repo.test.ts src/api/routes/job-status.test.ts src/api/routes/jobs.test.ts src/server.test.ts -x` passes.
- Polling `/jobs/:id` shows lifecycle transitions and non-stale heartbeat/progress counters during execution.
- Unknown job IDs return `404` with machine-readable not-found response body.
</verification>

<success_criteria>
- Jobs transition through `queued`, `running`, and terminal states with persisted timestamps.
- Progress counters and heartbeat values are available while jobs are executing.
- `GET /jobs/:id` provides a durable read model suitable for near-real-time polling visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-intake-and-visibility/02-job-intake-and-visibility-02-SUMMARY.md`
</output>
