---
phase: 02-job-intake-and-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/schemas/job-input.ts
  - src/orchestration/intake/normalize-intake.ts
  - src/api/routes/jobs.ts
  - src/api/routes/jobs.test.ts
autonomous: true
requirements:
  - INPT-01
  - INPT-02
  - INPT-03
must_haves:
  truths:
    - "User can submit a job with keyword and location and receive a queued job ID."
    - "User can submit a job using a canonical Google Maps search URL and receive a queued job ID."
    - "User can submit a job using place ID input and receive a queued job ID."
  artifacts:
    - path: "src/api/schemas/job-input.ts"
      provides: "Discriminated intake schema for keyword/location, maps_url, and place_id payload variants"
      exports: ["jobInputSchema", "JobInput"]
    - path: "src/orchestration/intake/normalize-intake.ts"
      provides: "Normalization from intake variants to one canonical internal target"
      exports: ["normalizeIntakeInput"]
    - path: "src/api/routes/jobs.ts"
      provides: "POST /jobs variant-aware intake handling while preserving async 202 acceptance"
      exports: ["registerJobRoutes"]
  key_links:
    - from: "src/api/routes/jobs.ts"
      to: "src/api/schemas/job-input.ts"
      via: "payload validation using discriminated schema"
      pattern: "inputType|jobInputSchema"
    - from: "src/api/routes/jobs.ts"
      to: "src/orchestration/intake/normalize-intake.ts"
      via: "canonical target normalization before persistence"
      pattern: "normalizeIntakeInput"
    - from: "src/api/routes/jobs.test.ts"
      to: "src/api/routes/jobs.ts"
      via: "route integration tests for all intake variants"
      pattern: "POST /jobs|maps_url|place_id"
---

<objective>
Expand intake contracts so `/jobs` accepts every v1 input format and normalizes each request into one canonical internal target before queue persistence.

Purpose: Satisfy INPT-01/02/03 without creating input-specific execution branches.
Output: Variant-safe intake schema, normalization utility, and POST route/test coverage for all supported input types.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-job-intake-and-visibility/02-RESEARCH.md
@.planning/phases/01-local-runtime-safety-baseline/01-local-runtime-safety-baseline-01-SUMMARY.md
@src/api/schemas/job-input.ts
@src/api/routes/jobs.ts
@src/api/routes/jobs.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed multi-format intake schema and canonical normalization</name>
  <files>src/api/schemas/job-input.ts, src/orchestration/intake/normalize-intake.ts</files>
  <action>Replace the single-shape job input schema with a discriminated union keyed by `inputType` supporting `keyword_location`, `maps_url`, and `place_id`. Require variant-specific fields only, reject ambiguous payloads, and keep policy/sensitive-field options available across variants. Add `normalizeIntakeInput` that converts each variant to one canonical internal target shape consumed by job persistence and runtime, parsing canonical Maps URLs (`/maps/search/?api=1&query=...` with optional `query_place_id`) into normalized query/place fields. Do not add fallback heuristics for unsupported URL forms; return actionable validation errors instead.</action>
  <verify>
    <automated>npm run test -- src/api/routes/jobs.test.ts -x</automated>
    <manual>Optional: submit one payload per inputType and confirm all are accepted with 202 while invalid mixed-shape payloads return 400.</manual>
    <sampling_rate>run after schema and normalization helper are in place</sampling_rate>
  </verify>
  <done>Schema enforces exactly one intake variant per request, and normalization returns one canonical target shape for all supported variants.</done>
</task>

<task type="auto">
  <name>Task 2: Wire POST /jobs to variant-aware intake and prove all v1 inputs</name>
  <files>src/api/routes/jobs.ts, src/api/routes/jobs.test.ts</files>
  <action>Update `POST /jobs` to parse discriminated intake payloads, call `normalizeIntakeInput`, and persist queued jobs through existing repo flow while preserving async `202` semantics and guardrail behavior. Ensure response still includes `jobId`, `status`, resolved policy, and guardrail metadata, and include enough normalized input metadata for downstream worker visibility. Extend route integration tests to cover: keyword/location intake, maps URL intake, place ID intake, and unsupported maps URL rejection. Keep endpoint path and response status contract stable for existing callers.</action>
  <verify>
    <automated>npm run test -- src/api/routes/jobs.test.ts src/api/routes/jobs.guardrails.test.ts -x</automated>
    <manual>Optional: `curl -X POST /jobs` for each inputType and confirm a `jobId` is returned for valid payloads.</manual>
    <sampling_rate>run after route wiring and test updates</sampling_rate>
  </verify>
  <done>All three v1 intake formats are accepted and queued consistently, with invalid variant payloads rejected via 400 responses.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/api/routes/jobs.test.ts src/api/routes/jobs.guardrails.test.ts -x` passes.
- Manual API smoke confirms `keyword_location`, `maps_url`, and `place_id` payloads each return `202` with `jobId`.
- Unsupported/ambiguous payloads fail validation with actionable 400 responses.
</verification>

<success_criteria>
- `POST /jobs` supports all v1 intake variants behind one typed contract.
- Intake normalization happens once at ingress, not across downstream execution branches.
- Existing async queued acceptance behavior remains intact (`202` + queued job ID).
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-intake-and-visibility/02-job-intake-and-visibility-01-SUMMARY.md`
</output>
