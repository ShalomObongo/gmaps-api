---
phase: 03-coverage-and-deduplication
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/places-repo.ts
  - src/storage/places-repo.test.ts
  - src/orchestration/coverage/place-key.ts
  - src/crawler/maps/collect-places.ts
  - src/crawler/maps/collect-places.test.ts
autonomous: true
requirements:
  - COVR-01
  - COVR-03
must_haves:
  truths:
    - "User can collect place candidates beyond the first visible viewport/page when limits allow deeper traversal."
    - "User receives one unique place row per logical place even when overlap or repeated discovery occurs."
  artifacts:
    - path: "src/crawler/maps/collect-places.ts"
      provides: "Bounded deep traversal loop with scroll growth checks and stop conditions"
      exports: ["collectPlacesFromMaps"]
    - path: "src/storage/places-repo.ts"
      provides: "Place ingest/read repository with SQLite-backed dedup writes"
      exports: ["createPlacesRepo"]
    - path: "src/storage/db.ts"
      provides: "Places table with unique dedup constraint per job"
      contains: "UNIQUE(job_id, place_key)"
  key_links:
    - from: "src/crawler/maps/collect-places.ts"
      to: "src/api/schemas/job-input.ts"
      via: "collector contract consumes persisted run controls from validated intake schema"
      pattern: "maxPlaces|maxScrollSteps|maxViewportPans"
    - from: "src/orchestration/coverage/place-key.ts"
      to: "src/storage/places-repo.ts"
      via: "repository writes canonical place keys on every insert/upsert"
      pattern: "placeKey|canonical|hash"
    - from: "src/storage/places-repo.ts"
      to: "src/storage/db.ts"
      via: "SQLite unique index enforces hard dedup correctness"
      pattern: "UNIQUE\(job_id, place_key\)|ON CONFLICT"
---

<objective>
Implement bounded deep collection and write-time deduplication so runs gather more than first-page results while guaranteeing unique place outputs.

Purpose: Satisfy core COVR-01/COVR-03 foundations by implementing bounded traversal mechanics and durable write-time dedup storage.
Output: Deep traversal collector, deduplicating places repository + schema constraints, and tests covering limits plus overlap dedup behavior.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-coverage-and-deduplication/03-RESEARCH.md
@.planning/phases/03-coverage-and-deduplication/03-01-PLAN.md
@src/storage/jobs-repo.ts
@src/storage/db.ts
@src/storage/places-repo.ts
@src/crawler/maps/collect-places.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical place-key dedup storage with SQLite uniqueness guarantees</name>
  <files>src/storage/schema.ts, src/storage/db.ts, src/storage/places-repo.ts, src/storage/places-repo.test.ts, src/orchestration/coverage/place-key.ts</files>
  <action>Create place persistence primitives for run outputs: add typed place record model, create `places` table, and enforce `UNIQUE(job_id, place_key)` with write path using `ON CONFLICT DO NOTHING` (or equivalent no-op upsert). Implement canonical key derivation with priority `placeId` and deterministic fallback hash from normalized identity/location tuple when place ID is missing. Keep dedup correctness in the database as the source of truth; in-memory dedup may short-circuit writes but must not replace storage-level guarantees. Add repository tests proving duplicate candidates for the same job do not create multiple rows while distinct jobs can store same place key independently.</action>
  <verify>
    <automated>npm run test -- src/storage/places-repo.test.ts -x</automated>
    <manual>Optional: inspect SQLite `places` table after duplicate inserts and confirm one row per `(job_id, place_key)`.</manual>
    <sampling_rate>run after places schema/repo implementation</sampling_rate>
  </verify>
  <done>Place writes are deduplicated durably per job via canonical place keys and SQLite uniqueness constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded deep collection beyond initial viewport with explicit stop conditions</name>
  <files>src/crawler/maps/collect-places.ts, src/crawler/maps/collect-places.test.ts</files>
  <action>Implement a traversal executor that continues discovery beyond first viewport using deterministic scroll/pagination mechanics and optional bounded viewport panning. Drive behavior from persisted controls (`maxPlaces` required, optional `maxScrollSteps`, `maxViewportPans`) and stop on explicit conditions: reached place cap, exhausted scroll growth budget, exhausted viewport pan budget, or no-growth streak threshold. Avoid unbounded while-loops and blind sleep-only progression; use growth checks after each scroll step to detect stalled discovery. Add focused tests for limit enforcement and no-growth termination semantics so traversal behavior remains predictable.</action>
  <verify>
    <automated>npm run test -- src/crawler/maps/collect-places.test.ts -x</automated>
    <manual>Run one deep job with low and high `maxPlaces`; confirm discovered candidates differ and traversal stops at configured bounds.</manual>
    <sampling_rate>run after collector and traversal-limit tests are added</sampling_rate>
  </verify>
  <done>Collector can discover candidates beyond first viewport while honoring explicit depth limits and deterministic stop conditions.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/storage/places-repo.test.ts src/crawler/maps/collect-places.test.ts -x` passes.
- Runs with higher limits collect more candidates than low-limit runs, without infinite traversal behavior.
- Duplicate place candidates across overlapping scans persist as single unique rows per job.
</verification>

<success_criteria>
- Jobs can traverse beyond first viewport/page under explicit depth bounds.
- Overlapping scans produce deduplicated place outputs per run.
- Traversal and dedup foundations are ready for worker/status integration in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coverage-and-deduplication/03-coverage-and-deduplication-02-SUMMARY.md`
</output>
