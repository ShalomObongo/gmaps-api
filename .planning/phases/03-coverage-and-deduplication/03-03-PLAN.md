---
phase: 03-coverage-and-deduplication
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/orchestration/runner/jobs-worker.ts
  - src/orchestration/runner/jobs-worker.test.ts
  - src/api/routes/job-status.ts
  - src/api/routes/job-status.test.ts
autonomous: true
requirements:
  - COVR-03
must_haves:
  truths:
    - "User receives one unique place row per logical place even when overlap or repeated discovery occurs."
    - "Run progress clearly reflects both discovered candidates and unique accepted places."
    - "Completed job status reports deduplicated totals derived from persisted place writes."
  artifacts:
    - path: "src/orchestration/runner/jobs-worker.ts"
      provides: "Worker wiring that executes deep collection and writes deduplicated progress"
      exports: ["createJobsWorker"]
    - path: "src/api/routes/job-status.ts"
      provides: "Status response fields exposing discovered-versus-unique place progress"
      exports: ["registerJobStatusRoutes"]
  key_links:
    - from: "src/orchestration/runner/jobs-worker.ts"
      to: "src/crawler/maps/collect-places.ts"
      via: "worker executes bounded collector using persisted collection controls"
      pattern: "collectPlacesFromMaps|maxPlaces|maxScrollSteps|maxViewportPans"
    - from: "src/orchestration/runner/jobs-worker.ts"
      to: "src/storage/places-repo.ts"
      via: "worker writes candidates through deduplicating repository and tracks unique accepts"
      pattern: "createPlacesRepo|insert|upsert|unique"
    - from: "src/api/routes/job-status.ts"
      to: "src/orchestration/runner/jobs-worker.ts"
      via: "status output uses worker-persisted counters for discovered and unique totals"
      pattern: "discovered|unique|processedCount"
---

<objective>
Integrate worker execution and status observability on top of the new traversal and dedup foundations.

Purpose: Complete COVR-03 user-visible behavior by ensuring runtime metrics and status payloads reflect deduplicated place ingestion.
Output: Worker pipeline wiring to collector + places repo, status response updates, and tests proving discovered-versus-unique progress accuracy.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-coverage-and-deduplication/03-RESEARCH.md
@.planning/phases/03-coverage-and-deduplication/03-01-PLAN.md
@.planning/phases/03-coverage-and-deduplication/03-02-PLAN.md
@src/orchestration/runner/jobs-worker.ts
@src/storage/jobs-repo.ts
@src/storage/places-repo.ts
@src/crawler/maps/collect-places.ts
@src/api/routes/job-status.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire jobs worker to deep collector plus deduplicating places repository</name>
  <files>src/orchestration/runner/jobs-worker.ts, src/orchestration/runner/jobs-worker.test.ts</files>
  <action>Replace placeholder worker execution with the real ingestion pipeline: load persisted collection controls from each queued job, execute the deep collector, and write each candidate through places repository dedup semantics. Maintain separate counters for raw discovered candidates and unique accepted inserts so overlap behavior stays measurable. Add worker tests using deterministic collector fixtures (including duplicates) to prove final counters and completion behavior are correct under bounded limits.</action>
  <verify>
    <automated>npm run test -- src/orchestration/runner/jobs-worker.test.ts -x</automated>
    <manual>Run one overlapped scan job and confirm worker-completed metrics show discovered count greater than or equal to unique accepted count.</manual>
    <sampling_rate>run after worker pipeline and tests are updated</sampling_rate>
  </verify>
  <done>Worker executes the deep collector and persists deduplicated places while tracking discovered-versus-unique counters.</done>
</task>

<task type="auto">
  <name>Task 2: Expose dedup-aware progress and completion metrics in job status responses</name>
  <files>src/api/routes/job-status.ts, src/api/routes/job-status.test.ts</files>
  <action>Update `GET /jobs/:id` status payload to expose both discovered candidate totals and unique accepted place totals sourced from persisted job progress. Keep response compatibility for existing fields while adding explicit dedup-aware metrics for operator visibility. Extend status route tests to assert metric presence and correctness across queued, running, and completed snapshots.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-status.test.ts -x</automated>
    <manual>Poll a running job and then its completed state; verify status includes both discovered and unique totals and unique does not exceed discovered.</manual>
    <sampling_rate>run after status route updates</sampling_rate>
  </verify>
  <done>Status endpoint consistently surfaces dedup-aware run metrics derived from persisted worker progress.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/orchestration/runner/jobs-worker.test.ts src/api/routes/job-status.test.ts -x` passes.
- Completed overlap-heavy runs report discovered totals greater than or equal to unique totals.
- Status payload surfaces dedup-aware counters without regressing existing lifecycle reporting.
</verification>

<success_criteria>
- Worker pipeline uses persisted controls and writes candidates through dedup storage.
- Run metrics distinguish raw discovery from unique accepted outputs.
- Users can verify dedup behavior directly from `GET /jobs/:id` status responses.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coverage-and-deduplication/03-coverage-and-deduplication-03-SUMMARY.md`
</output>
