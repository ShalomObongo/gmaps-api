---
phase: 03-coverage-and-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/schemas/job-input.ts
  - src/orchestration/intake/normalize-intake.ts
  - src/api/routes/jobs.ts
  - src/api/routes/jobs.test.ts
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/jobs-repo.ts
  - src/storage/jobs-repo.test.ts
  - src/api/routes/job-status.ts
  - src/api/routes/job-status.test.ts
autonomous: true
requirements:
  - COVR-02
must_haves:
  truths:
    - "User can submit a job with explicit collection depth/limit controls."
    - "User can see that the run is using persisted collection controls, not transient request-only values."
    - "Different submitted limits produce different persisted run configuration for downstream worker execution."
  artifacts:
    - path: "src/api/schemas/job-input.ts"
      provides: "Typed intake validation for depth controls (`maxPlaces` and optional bounded knobs)"
      contains: "collection control schema merged into job input variants"
    - path: "src/api/routes/jobs.ts"
      provides: "POST /jobs persistence of collection controls and response echo"
      exports: ["registerJobRoutes"]
    - path: "src/storage/jobs-repo.ts"
      provides: "Job persistence that stores and reads collection control JSON"
      exports: ["createJobsRepo"]
    - path: "src/storage/db.ts"
      provides: "SQLite schema migration for run-level collection config storage"
      contains: "collection_config_json column"
  key_links:
    - from: "src/api/routes/jobs.ts"
      to: "src/storage/jobs-repo.ts"
      via: "create job call persists collection controls with the queued record"
      pattern: "collectionConfig|collection_config_json"
    - from: "src/api/schemas/job-input.ts"
      to: "src/orchestration/intake/normalize-intake.ts"
      via: "validated depth fields become normalized worker-ready controls"
      pattern: "maxPlaces|maxScrollSteps|maxViewportPans"
    - from: "src/api/routes/job-status.ts"
      to: "src/storage/jobs-repo.ts"
      via: "status payload includes persisted collection controls for observability"
      pattern: "collectionConfig"
---

<objective>
Introduce typed, persisted collection depth controls so every job carries an explicit run limit contract the worker can trust.

Purpose: Satisfy COVR-02 by making depth/limit behavior configurable at intake and durable through job lifecycle storage.
Output: Extended job input schema, route/repo wiring for persisted collection controls, and tests proving controls persist and remain observable.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-coverage-and-deduplication/03-RESEARCH.md
@.planning/phases/02-job-intake-and-visibility/02-job-intake-and-visibility-01-SUMMARY.md
@.planning/phases/02-job-intake-and-visibility/02-job-intake-and-visibility-02-SUMMARY.md
@src/api/schemas/job-input.ts
@src/api/routes/jobs.ts
@src/storage/jobs-repo.ts
@src/storage/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed collection depth controls to intake normalization and acceptance responses</name>
  <files>src/api/schemas/job-input.ts, src/orchestration/intake/normalize-intake.ts, src/api/routes/jobs.ts, src/api/routes/jobs.test.ts</files>
  <action>Extend intake schema with explicit collection controls: required primary limit `maxPlaces` plus optional bounded controls (`maxScrollSteps`, `maxViewportPans`) with safe min/max clamps. Normalize these fields with the intake target so worker-facing config is shape-stable for all input variants (`keyword_location`, `maps_url`, `place_id`). Update `POST /jobs` to return accepted collection controls in the `202` payload. Keep existing validation behavior and error shape; reject out-of-range controls at the API boundary (do not silently coerce invalid values).</action>
  <verify>
    <automated>npm run test -- src/api/routes/jobs.test.ts -x</automated>
    <manual>POST two jobs with different `maxPlaces` values and confirm accepted payloads reflect the submitted limits.</manual>
    <sampling_rate>run after intake schema and route updates</sampling_rate>
  </verify>
  <done>Job submission accepts depth/limit controls, rejects invalid values, and returns normalized control values in the acceptance payload.</done>
</task>

<task type="auto">
  <name>Task 2: Persist collection controls in SQLite job records and expose them in status snapshots</name>
  <files>src/storage/schema.ts, src/storage/db.ts, src/storage/jobs-repo.ts, src/storage/jobs-repo.test.ts, src/api/routes/job-status.ts, src/api/routes/job-status.test.ts</files>
  <action>Add durable job-level storage for collection controls (for example `collection_config_json`) and wire repository create/read paths to persist and hydrate typed config. Include migration-safe schema evolution in `db.ts` so existing local databases upgrade without destructive resets. Expose persisted collection controls via `GET /jobs/:id` status response so operators can verify the worker is using stored values, not request-body state. Extend repository and status route tests to prove controls persist through queued -> running -> completed lifecycle snapshots.</action>
  <verify>
    <automated>npm run test -- src/storage/jobs-repo.test.ts src/api/routes/job-status.test.ts -x</automated>
    <manual>Submit a job with custom controls, then poll `/jobs/:id` and confirm the same control values appear in the response before and after transition to running.</manual>
    <sampling_rate>run after schema/repo/status wiring</sampling_rate>
  </verify>
  <done>Collection controls are durable per job record and visible from status reads across lifecycle transitions.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/api/routes/jobs.test.ts src/storage/jobs-repo.test.ts src/api/routes/job-status.test.ts -x` passes.
- Invalid collection control values are rejected with a machine-readable `400` invalid request response.
- Persisted control values are visible via `GET /jobs/:id` and remain stable after worker transitions.
</verification>

<success_criteria>
- User can configure per-run depth/limit controls at job submission time.
- Job storage persists those controls durably for worker consumption.
- Status endpoint reflects stored controls so run depth behavior is inspectable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coverage-and-deduplication/03-coverage-and-deduplication-01-SUMMARY.md`
</output>
