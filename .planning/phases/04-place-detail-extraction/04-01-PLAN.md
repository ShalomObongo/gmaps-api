---
phase: 04-place-detail-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/crawler/maps/normalize-place-record.ts
  - src/crawler/maps/normalize-place-record.test.ts
  - src/storage/schema.ts
  - src/storage/db.ts
  - src/storage/places-repo.ts
  - src/storage/places-repo.test.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "User receives every place with the same core fields, even when some values are unavailable."
    - "User receives name/category/rating/reviews/address/coordinates in the stored place record contract."
    - "Previously created local databases continue to work after adding new core detail fields."
  artifacts:
    - path: "src/crawler/maps/normalize-place-record.ts"
      provides: "Strict normalized place schema with explicit nullable core detail fields"
      exports: ["normalizePlaceRecord", "PlaceRecordSchema"]
    - path: "src/storage/schema.ts"
      provides: "Typed place candidate/record contracts including core detail fields"
      contains: "category, rating, reviewsCount"
    - path: "src/storage/db.ts"
      provides: "Migration-safe SQLite columns for core place details"
      contains: "category, rating, reviews_count"
    - path: "src/storage/places-repo.ts"
      provides: "Insert/list persistence wiring for normalized core detail fields"
      exports: ["createPlacesRepo"]
  key_links:
    - from: "src/crawler/maps/normalize-place-record.ts"
      to: "src/storage/places-repo.ts"
      via: "worker/repo insert path only accepts normalized nullable core fields"
      pattern: "normalizePlaceRecord|reviewsCount|category|rating"
    - from: "src/storage/db.ts"
      to: "src/storage/places-repo.ts"
      via: "column definitions match repository insert/select aliases"
      pattern: "reviews_count|category|rating"
---

<objective>
Establish a strict, nullable core place detail contract and persist it durably so every stored place record has a consistent DATA-01 shape.

Purpose: Satisfy DATA-01 with stable normalized core fields that remain safe on partial extraction and existing local DB upgrades.
Output: New normalization module, extended place types, additive SQLite columns, repository persistence updates, and focused contract tests.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-place-detail-extraction/04-RESEARCH.md
@.planning/phases/03-coverage-and-deduplication/03-coverage-and-deduplication-02-SUMMARY.md
@src/storage/schema.ts
@src/storage/db.ts
@src/storage/places-repo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict normalized core place detail schema with explicit nullability</name>
  <files>src/crawler/maps/normalize-place-record.ts, src/crawler/maps/normalize-place-record.test.ts, src/storage/schema.ts</files>
  <action>Create a dedicated normalization module that converts raw/partial place detail values into a strict record shape for DATA-01 fields: `name`, `category`, `rating`, `reviewsCount`, `address`, `lat`, `lng`, plus existing identity fields. Use Zod strict-object validation and explicit `null` outputs for unavailable optional values (do not omit keys and do not throw for missing optional fields). Keep parse failures for optional numeric fields (`rating`, `reviewsCount`, coordinates) as nullable outcomes so extraction remains best-effort but contract-stable.</action>
  <verify>
    <automated>npm run test -- src/crawler/maps/normalize-place-record.test.ts -x</automated>
    <manual>Run normalization on one complete fixture and one sparse fixture; both outputs include identical keys with nullable values where data is missing.</manual>
    <sampling_rate>run after normalization module and schema typing changes</sampling_rate>
  </verify>
  <done>Normalized place objects always include DATA-01 core fields with strict, shape-stable nullability and tests cover full + sparse inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Persist normalized core detail fields with additive SQLite migration safety</name>
  <files>src/storage/db.ts, src/storage/places-repo.ts, src/storage/places-repo.test.ts, src/storage/schema.ts</files>
  <action>Extend `places` persistence to store/retrieve core detail columns (`category`, `rating`, `reviews_count`) alongside existing identity/location fields. Add idempotent `ensureColumn` calls so existing databases are upgraded without reset, and update repository insert/select aliasing to keep TypeScript output shape consistent with the normalized contract. Preserve dedup behavior (`UNIQUE(job_id, place_key)` with `ON CONFLICT DO NOTHING`) and avoid changing key generation semantics from Phase 3.</action>
  <verify>
    <automated>npm run test -- src/storage/places-repo.test.ts -x</automated>
    <manual>Create an old-schema database, re-open through `createDatabase`, and confirm inserts/reads succeed with new nullable columns present.</manual>
    <sampling_rate>run after db/repository migration and query updates</sampling_rate>
  </verify>
  <done>Core detail fields are durably persisted and returned in repository reads, and migration path remains safe for pre-Phase-4 SQLite files.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/crawler/maps/normalize-place-record.test.ts src/storage/places-repo.test.ts -x` passes.
- Place records persisted through repository reads include stable DATA-01 fields with nullable fallbacks instead of missing keys.
- Existing local SQLite files upgrade in place and continue accepting new place inserts.
</verification>

<success_criteria>
- Every stored place record has consistent DATA-01 shape: name/category/rating/reviews count/address/coordinates.
- Core detail field extraction failures for optional values degrade to `null`, not record loss.
- Storage contract is migration-safe and backward-compatible with existing local databases.
</success_criteria>

<output>
After completion, create `.planning/phases/04-place-detail-extraction/04-place-detail-extraction-01-SUMMARY.md`
</output>
