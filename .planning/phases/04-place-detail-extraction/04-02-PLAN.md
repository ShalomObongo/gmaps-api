---
phase: 04-place-detail-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/crawler/maps/extract-place-details.ts
  - src/crawler/maps/extract-place-details.test.ts
  - src/orchestration/runner/jobs-worker.ts
  - src/orchestration/runner/jobs-worker.test.ts
  - src/storage/schema.ts
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "User receives website/phone/opening-hours fields when those details are present on a listing."
    - "User still receives a valid place record when contact/business fields are missing."
    - "Worker-written place objects keep one consistent structure across all records in the same run."
  artifacts:
    - path: "src/crawler/maps/extract-place-details.ts"
      provides: "Locator-first place detail extraction with fallback selectors and nullable outputs"
      exports: ["extractPlaceDetails"]
    - path: "src/orchestration/runner/jobs-worker.ts"
      provides: "Worker wiring from collected candidates through detail extraction and normalization before persistence"
      exports: ["createJobsWorker"]
    - path: "src/storage/schema.ts"
      provides: "Typed contact/business fields in place candidate/record contracts"
      contains: "website, phone, openingHoursJson"
  key_links:
    - from: "src/crawler/maps/extract-place-details.ts"
      to: "src/orchestration/runner/jobs-worker.ts"
      via: "worker invokes detail extraction and merges results into normalized candidate records"
      pattern: "extractPlaceDetails|website|phone|openingHoursJson"
    - from: "src/orchestration/runner/jobs-worker.ts"
      to: "src/storage/places-repo.ts"
      via: "persisted candidates contain contact/business fields without breaking dedup completion flow"
      pattern: "placesRepo.insert|openingHoursJson|website|phone"
---

<objective>
Wire best-effort contact/business detail extraction into the worker flow so runs produce consistently shaped place records enriched with DATA-02 fields when available.

Purpose: Satisfy DATA-02 while preserving reliable completion behavior under partial detail availability.
Output: New detail extractor module with fallback selectors, worker integration that normalizes nullable contact fields, and tests for present/missing detail scenarios.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-place-detail-extraction/04-RESEARCH.md
@.planning/phases/03-coverage-and-deduplication/03-coverage-and-deduplication-03-SUMMARY.md
@.planning/phases/04-place-detail-extraction/04-01-PLAN.md
@src/orchestration/runner/jobs-worker.ts
@src/crawler/maps/collect-places.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement locator-first detail extraction with fallback selectors for contact/business fields</name>
  <files>src/crawler/maps/extract-place-details.ts, src/crawler/maps/extract-place-details.test.ts, src/storage/schema.ts</files>
  <action>Create a dedicated detail extraction module for website, phone, and opening hours using Playwright locator-first patterns with fallback selector map for Google Maps UI variations. Return nullable fields (`website`, `phone`, `openingHoursJson`) and keep failures isolated to the field level (do not fail the whole extraction when one field is missing or unparsable). Represent hours as canonical JSON string for now (`openingHoursJson`) per research recommendation; avoid relational or schema-heavy hours modeling in this phase.</action>
  <verify>
    <automated>npm run test -- src/crawler/maps/extract-place-details.test.ts -x</automated>
    <manual>Run extractor against one place with visible website/phone/hours and one place missing those fields; both return the same key set with nullable fallbacks.</manual>
    <sampling_rate>run after extractor implementation and fallback selector tests</sampling_rate>
  </verify>
  <done>Detail extractor returns stable nullable contact/business fields with fallback selector coverage for common DOM variants.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate detail extraction into worker persistence path without degrading run reliability</name>
  <files>src/orchestration/runner/jobs-worker.ts, src/orchestration/runner/jobs-worker.test.ts, src/storage/schema.ts</files>
  <action>Update worker collector execution to enrich discovered places through the Phase 4 normalization/extractor flow before insert. Ensure every persisted record keeps one consistent shape containing both DATA-01 and DATA-02 fields, with explicit nullable values when details are absent. Preserve existing completion and metric behavior (`discoveredCount`, `uniqueAcceptedCount`) and avoid introducing hard failure paths for optional detail extraction misses.</action>
  <verify>
    <automated>npm run test -- src/orchestration/runner/jobs-worker.test.ts -x</automated>
    <manual>Execute a job with mixed-detail candidates and confirm job completes with dedup metrics intact while persisted records include nullable contact fields.</manual>
    <sampling_rate>run after worker integration and regression tests</sampling_rate>
  </verify>
  <done>Worker writes consistently structured enriched place records and remains resilient when optional contact/business fields are unavailable.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/crawler/maps/extract-place-details.test.ts src/orchestration/runner/jobs-worker.test.ts -x` passes.
- Worker completion remains successful for mixed-detail runs and no regressions in dedup-aware progress metrics.
- Persisted place rows include website/phone/opening hours fields when present, otherwise explicit `null` values.
</verification>

<success_criteria>
- DATA-02 fields are extracted and persisted when available on source listings.
- Missing optional contact/business fields do not fail records or runs.
- Place objects remain consistently shaped across all records in a run.
</success_criteria>

<output>
After completion, create `.planning/phases/04-place-detail-extraction/04-place-detail-extraction-02-SUMMARY.md`
</output>
