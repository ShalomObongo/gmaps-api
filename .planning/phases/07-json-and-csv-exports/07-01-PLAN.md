---
phase: 07-json-and-csv-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/output/build-job-results-model.ts
  - src/output/build-job-results-model.test.ts
  - src/output/serializers/job-results-csv.ts
  - src/output/serializers/job-results-csv.test.ts
  - src/api/routes/job-results.ts
  - src/api/routes/job-exports.ts
  - src/api/routes/job-exports.test.ts
  - src/server.ts
autonomous: true
requirements:
  - OUTP-02
  - OUTP-03
must_haves:
  truths:
    - "User can download completed job results as a JSON file with a stable filename and attachment headers."
    - "User can download the same completed job results as a CSV file with valid escaping for commas, quotes, and line breaks."
    - "Exports preserve existing state-gated behavior: missing jobs return 404, non-completed jobs return 409, completed jobs return 200."
  artifacts:
    - path: "src/api/routes/job-exports.ts"
      provides: "GET /jobs/:id/exports with format=json|csv and explicit download response headers"
      exports: ["registerJobExportsRoutes"]
    - path: "src/output/build-job-results-model.ts"
      provides: "Shared completed-job read model used by both results and export routes"
      exports: ["buildJobResultsModel"]
    - path: "src/output/serializers/job-results-csv.ts"
      provides: "CSV serialization using @fast-csv/format with deterministic headers and escaped values"
      exports: ["serializeJobResultsCsv"]
    - path: "src/api/routes/job-exports.test.ts"
      provides: "Contract coverage for JSON and CSV exports including headers and CSV escaping"
      contains: "GET /jobs/:id/exports"
  key_links:
    - from: "src/api/routes/job-exports.ts"
      to: "src/output/build-job-results-model.ts"
      via: "single completed-job source-of-truth assembly before format selection"
      pattern: "buildJobResultsModel"
    - from: "src/api/routes/job-exports.ts"
      to: "src/output/serializers/job-results-csv.ts"
      via: "csv format branch"
      pattern: "format === \"csv\"|serializeJobResultsCsv"
    - from: "src/server.ts"
      to: "src/api/routes/job-exports.ts"
      via: "server bootstrap route registration"
      pattern: "registerJobExportsRoutes"
---

<objective>
Implement downloadable JSON and CSV export endpoints for completed jobs without changing crawl/runtime behavior.

Purpose: OUTP-02 and OUTP-03 require file-oriented outputs for developer data workflows while preserving the state-gated semantics established in Phase 6.
Output: A shared completed-job output model, serializer modules for JSON/CSV, and a `/jobs/:id/exports` route that returns attachment responses with deterministic filenames.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-json-and-csv-exports/07-RESEARCH.md
@.planning/phases/06-results-api-access/06-results-api-access-01-SUMMARY.md
@src/api/routes/job-results.ts
@src/api/routes/job-results.test.ts
@src/storage/jobs-repo.ts
@src/storage/places-repo.ts
@src/storage/place-reviews-repo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared completed-job results model builder and refactor existing results route to use it</name>
  <files>src/output/build-job-results-model.ts, src/output/build-job-results-model.test.ts, src/api/routes/job-results.ts, src/api/routes/job-results.test.ts</files>
  <action>Extract Phase 6 completed-job assembly logic into `buildJobResultsModel(jobId, deps)` so results and exports use one source of truth. Keep existing `/jobs/:id/results` API contract unchanged (`404` missing, `409` non-completed, `200` completed) while replacing duplicated in-route mapping with the shared builder output. Ensure deterministic ordering and nested review grouping remain identical to current behavior.</action>
  <verify>
    <automated>npm run test -- src/output/build-job-results-model.test.ts src/api/routes/job-results.test.ts -x</automated>
    <manual>Optional: compare `/jobs/:id/results` payload before/after for a seeded completed job and confirm structure and order remain unchanged.</manual>
    <sampling_rate>run after model extraction and route refactor</sampling_rate>
  </verify>
  <done>Completed-job output assembly is centralized in one tested builder, and `/jobs/:id/results` behavior remains contract-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Implement export route with JSON and CSV serializers plus attachment semantics</name>
  <files>package.json, package-lock.json, src/output/serializers/job-results-csv.ts, src/output/serializers/job-results-csv.test.ts, src/api/routes/job-exports.ts, src/api/routes/job-exports.test.ts</files>
  <action>Install `@fast-csv/format` and create `GET /jobs/:id/exports` with query contract `format=json|csv` (default `json`). Reuse the shared completed-job model builder from Task 1; do not duplicate read-model joins in the route. For JSON, return `application/json` with `Content-Disposition: attachment; filename="job-{id}.json"`. For CSV, serialize place-centric rows with deterministic headers and a `reviewsJson` column using `@fast-csv/format` (not hand-rolled escaping), return `text/csv; charset=utf-8` and `Content-Disposition: attachment; filename="job-{id}.csv"`. Keep the same state gate behavior as `/jobs/:id/results` for `404` and `409`.</action>
  <verify>
    <automated>npm run test -- src/output/serializers/job-results-csv.test.ts src/api/routes/job-exports.test.ts -x</automated>
    <manual>Optional: request both formats for one completed job and confirm browser/download clients receive attachment filenames and correct MIME types.</manual>
    <sampling_rate>run after serializer and route contract tests are added</sampling_rate>
  </verify>
  <done>Export endpoint returns downloadable JSON and CSV for completed jobs with correct headers, stable filenames, and standards-safe CSV escaping.</done>
</task>

<task type="auto">
  <name>Task 3: Register export route in server and run targeted regression suite</name>
  <files>src/server.ts, src/api/routes/job-exports.test.ts</files>
  <action>Register `registerJobExportsRoutes` in `buildServer` using existing repository instances so `/jobs/:id`, `/jobs/:id/results`, and `/jobs/:id/exports` coexist. Extend assembled-server tests in `job-exports.test.ts` to verify route availability and non-regression of state-gated behavior alongside existing results route semantics.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-results.test.ts src/api/routes/job-exports.test.ts src/server.test.ts -x</automated>
    <manual>Optional: run local server and call export endpoint on queued and completed jobs to confirm `409` vs `200` behavior with attachments.</manual>
    <sampling_rate>run after server route registration</sampling_rate>
  </verify>
  <done>Server exposes export endpoints end-to-end with passing regression coverage for results and export contracts.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/output/build-job-results-model.test.ts src/output/serializers/job-results-csv.test.ts src/api/routes/job-results.test.ts src/api/routes/job-exports.test.ts src/server.test.ts -x` passes.
- `/jobs/:id/exports?format=json` and `/jobs/:id/exports?format=csv` return `200` only for completed jobs with attachment headers and deterministic filenames.
- CSV output is parseable with preserved field boundaries for values containing commas, quotes, and newlines.
</verification>

<success_criteria>
- OUTP-02 is satisfied: completed job results are downloadable as JSON files from API.
- OUTP-03 is satisfied: same completed job results are downloadable as CSV files with standards-safe escaping.
- Export and results endpoints share one completed-job read model so JSON API and file exports stay in sync.
</success_criteria>

<output>
After completion, create `.planning/phases/07-json-and-csv-exports/07-json-and-csv-exports-01-SUMMARY.md`
</output>
