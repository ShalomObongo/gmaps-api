---
phase: 07-json-and-csv-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output/build-job-results-model.ts
  - src/output/build-job-results-model.test.ts
  - src/api/routes/job-results.ts
  - src/api/routes/job-results.test.ts
autonomous: true
requirements:
  - OUTP-02
must_haves:
  truths:
    - "Completed-job output assembly exists as a single reusable read model for result and export routes."
    - "Current `/jobs/:id/results` behavior remains unchanged for 404, 409, and 200 paths after refactor."
  artifacts:
    - path: "src/output/build-job-results-model.ts"
      provides: "Shared completed-job read model used by both results and export routes"
      exports: ["buildJobResultsModel"]
    - path: "src/api/routes/job-results.ts"
      provides: "Results endpoint powered by shared completed-job model without contract drift"
      contains: "GET /jobs/:id/results"
    - path: "src/output/build-job-results-model.test.ts"
      provides: "Builder-level regression coverage for deterministic output assembly"
      contains: "buildJobResultsModel"
  key_links:
    - from: "src/api/routes/job-results.ts"
      to: "src/output/build-job-results-model.ts"
      via: "results handler delegates completed-job shaping to shared builder"
      pattern: "buildJobResultsModel"
    - from: "src/output/build-job-results-model.ts"
      to: "src/storage/jobs-repo.ts"
      via: "state-gated job lookup before output shaping"
      pattern: "getById"
---

<objective>
Establish a shared completed-job output model and refactor the existing results route onto it before export wiring.

Purpose: This foundation removes duplicate shaping logic so Phase 7 exports can reuse one source of truth while preserving Phase 6 API semantics.
Output: A tested `buildJobResultsModel` module and a contract-preserving `/jobs/:id/results` refactor that depends on it.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-results-api-access/06-results-api-access-01-SUMMARY.md
@src/api/routes/job-results.ts
@src/api/routes/job-results.test.ts
@src/storage/jobs-repo.ts
@src/storage/places-repo.ts
@src/storage/place-reviews-repo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared completed-job read model builder with deterministic output</name>
  <files>src/output/build-job-results-model.ts, src/output/build-job-results-model.test.ts, src/api/routes/job-results.ts, src/api/routes/job-results.test.ts</files>
  <action>Move completed-job shaping from `job-results` route into `buildJobResultsModel(jobId, deps)` and expose a typed return model suitable for both API JSON responses and file exports. Keep ordering and review nesting deterministic to match existing behavior. Add focused unit tests that cover missing/non-completed/completed gates and stable structure output for a completed job.</action>
  <verify>
    <automated>npm run test -- src/output/build-job-results-model.test.ts src/api/routes/job-results.test.ts -x</automated>
    <manual>Optional: compare `/jobs/:id/results` payload before/after for a seeded completed job and confirm structure and order remain unchanged.</manual>
    <sampling_rate>run after builder extraction and initial route migration</sampling_rate>
  </verify>
  <done>`buildJobResultsModel` is the only place that assembles completed-job output, and route behavior remains contract-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor results route to consume builder and run regression verification</name>
  <files>src/api/routes/job-results.ts, src/api/routes/job-results.test.ts, src/output/build-job-results-model.ts</files>
  <action>Update `GET /jobs/:id/results` to call `buildJobResultsModel` and return its completed payload without altering status semantics (`404`, `409`, `200`) or payload contract from Phase 6. Remove in-route duplication so Phase 7 export routes can reuse the same path in Plan 02. Keep test assertions explicit on state-gated responses and output shape to prevent silent contract drift.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-results.test.ts src/output/build-job-results-model.test.ts -x</automated>
    <manual>Optional: issue GET `/jobs/:id/results` for queued/completed job fixtures and confirm response status and payload parity with pre-refactor behavior.</manual>
    <sampling_rate>run after route wiring to shared builder</sampling_rate>
  </verify>
  <done>`/jobs/:id/results` is fully backed by `buildJobResultsModel`, preserving prior observable behavior and test coverage.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/output/build-job-results-model.test.ts src/api/routes/job-results.test.ts -x` passes.
- Refactored `/jobs/:id/results` endpoint remains behaviorally identical for missing, non-completed, and completed jobs.
</verification>

<success_criteria>
- Shared completed-job read model exists and is verified independently from route wiring.
- `/jobs/:id/results` semantics remain unchanged after refactor, making export implementation in the next plan low-risk.
</success_criteria>

<output>
After completion, create `.planning/phases/07-json-and-csv-exports/07-json-and-csv-exports-01-SUMMARY.md`
</output>
