---
phase: 07-json-and-csv-exports
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - package.json
  - package-lock.json
  - src/output/serializers/job-results-csv.ts
  - src/output/serializers/job-results-csv.test.ts
  - src/api/routes/job-exports.ts
  - src/api/routes/job-exports.test.ts
  - src/server.ts
autonomous: true
requirements:
  - OUTP-02
  - OUTP-03
must_haves:
  truths:
    - "User can download completed job results as JSON with attachment headers and stable filenames."
    - "User can download the same completed job results as CSV with standards-safe escaping for commas, quotes, and line breaks."
    - "Exports preserve state-gated behavior: missing jobs return 404, non-completed jobs return 409, completed jobs return 200."
  artifacts:
    - path: "src/api/routes/job-exports.ts"
      provides: "GET /jobs/:id/exports with format=json|csv and attachment response headers"
      exports: ["registerJobExportsRoutes"]
    - path: "src/output/serializers/job-results-csv.ts"
      provides: "CSV serialization using @fast-csv/format with deterministic headers and escaped values"
      exports: ["serializeJobResultsCsv"]
    - path: "src/api/routes/job-exports.test.ts"
      provides: "Route contract coverage for both formats and state gates"
      contains: "GET /jobs/:id/exports"
    - path: "src/server.ts"
      provides: "Route registration so exports are reachable from built server"
      contains: "registerJobExportsRoutes"
  key_links:
    - from: "src/api/routes/job-exports.ts"
      to: "src/output/build-job-results-model.ts"
      via: "shared completed-job model lookup before format branching"
      pattern: "buildJobResultsModel"
    - from: "src/api/routes/job-exports.ts"
      to: "src/output/serializers/job-results-csv.ts"
      via: "csv format branch serializer call"
      pattern: "format === \"csv\"|serializeJobResultsCsv"
    - from: "src/server.ts"
      to: "src/api/routes/job-exports.ts"
      via: "server bootstrap route registration"
      pattern: "registerJobExportsRoutes"
---

<objective>
Implement downloadable JSON and CSV export endpoints for completed jobs using the shared read model foundation from Plan 01.

Purpose: OUTP-02 and OUTP-03 require file-oriented outputs for developer data workflows with reliable attachment semantics and stable contracts.
Output: A tested `/jobs/:id/exports` route, CSV serializer module, dependency wiring for `@fast-csv/format`, and server registration.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-json-and-csv-exports/07-RESEARCH.md
@.planning/phases/07-json-and-csv-exports/07-json-and-csv-exports-01-SUMMARY.md
@src/output/build-job-results-model.ts
@src/api/routes/job-results.ts
@src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSV serializer and implement export route contract</name>
  <files>package.json, package-lock.json, src/output/serializers/job-results-csv.ts, src/output/serializers/job-results-csv.test.ts, src/api/routes/job-exports.ts, src/api/routes/job-exports.test.ts</files>
  <action>Install `@fast-csv/format` and implement `GET /jobs/:id/exports` with query contract `format=json|csv` (default `json`). Reuse `buildJobResultsModel` from Plan 01; do not duplicate read-model joins. For JSON responses, return `application/json` with `Content-Disposition: attachment; filename="job-{id}.json"`. For CSV, serialize place-centric rows with deterministic headers plus a `reviewsJson` column using `@fast-csv/format` (not hand-rolled escaping), and return `text/csv; charset=utf-8` with `Content-Disposition: attachment; filename="job-{id}.csv"`. Keep `404`/`409` behavior aligned with `/jobs/:id/results`.</action>
  <verify>
    <automated>npm run test -- src/output/serializers/job-results-csv.test.ts src/api/routes/job-exports.test.ts -x</automated>
    <manual>Optional: request both formats for one completed job and confirm attachment filename and MIME type in response headers.</manual>
    <sampling_rate>run after serializer and route tests are implemented</sampling_rate>
  </verify>
  <done>Export route returns downloadable JSON and CSV for completed jobs with deterministic filenames and standards-safe CSV escaping.</done>
</task>

<task type="auto">
  <name>Task 2: Register export route in server and run endpoint regression checks</name>
  <files>src/server.ts, src/api/routes/job-exports.test.ts</files>
  <action>Register `registerJobExportsRoutes` in `buildServer` with existing repository dependencies so `/jobs/:id`, `/jobs/:id/results`, and `/jobs/:id/exports` coexist. Extend assembled-server coverage in `job-exports.test.ts` to verify route availability and state-gated behavior for queued vs completed jobs.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-results.test.ts src/api/routes/job-exports.test.ts src/server.test.ts -x</automated>
    <manual>Optional: run local server and verify queued jobs return `409` while completed jobs return `200` with attachment headers for both formats.</manual>
    <sampling_rate>run after server route registration</sampling_rate>
  </verify>
  <done>Server exposes export endpoint end-to-end with passing regression coverage across existing and new output routes.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/output/serializers/job-results-csv.test.ts src/api/routes/job-exports.test.ts src/server.test.ts -x` passes.
- `/jobs/:id/exports?format=json` and `/jobs/:id/exports?format=csv` return attachment responses only when jobs are completed.
- CSV output remains parseable for values containing commas, quotes, and newlines.
</verification>

<success_criteria>
- OUTP-02 is satisfied: completed job results are downloadable as JSON files from API.
- OUTP-03 is satisfied: same completed job results are downloadable as CSV files with standards-safe escaping.
- Export route and results route stay synchronized through shared `buildJobResultsModel` wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/07-json-and-csv-exports/07-json-and-csv-exports-02-SUMMARY.md`
</output>
