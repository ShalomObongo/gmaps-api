---
phase: 06-results-api-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/place-reviews-repo.ts
  - src/storage/place-reviews-repo.test.ts
  - src/api/routes/job-results.ts
  - src/api/routes/job-results.test.ts
  - src/server.ts
autonomous: true
requirements:
  - OUTP-01
must_haves:
  truths:
    - "User can fetch completed job output through a dedicated API endpoint."
    - "User gets explicit non-success responses when a job is queued, running, failed, or missing."
    - "Completed results include stable, structured place data with nested ordered reviews."
  artifacts:
    - path: "src/api/routes/job-results.ts"
      provides: "GET /jobs/:id/results with state-gated 200/404/409 behavior and structured response contract"
      exports: ["registerJobResultsRoutes"]
    - path: "src/storage/place-reviews-repo.ts"
      provides: "Job-scoped ordered review reads to support single-pass results assembly"
      exports: ["createPlaceReviewsRepo"]
    - path: "src/api/routes/job-results.test.ts"
      provides: "Route contract coverage for completed and non-completed job states"
      contains: "GET /jobs/:id/results"
  key_links:
    - from: "src/api/routes/job-results.ts"
      to: "src/storage/jobs-repo.ts"
      via: "status gate that allows output reads only for completed jobs"
      pattern: "job\.status|completed|results_not_ready|not_found"
    - from: "src/api/routes/job-results.ts"
      to: "src/storage/places-repo.ts"
      via: "completed-job place read-model retrieval"
      pattern: "placesRepo\.listByJob"
    - from: "src/api/routes/job-results.ts"
      to: "src/storage/place-reviews-repo.ts"
      via: "single query review retrieval grouped by placeKey"
      pattern: "placeReviewsRepo\.listByJob|Map<"
---

<objective>
Implement a dedicated results read endpoint so completed run output is retrievable as a structured API contract.

Purpose: OUTP-01 requires durable output access semantics separate from lifecycle polling, with clear behavior across completed, in-progress, failed, and missing jobs.
Output: A new `/jobs/:id/results` route, supporting repository read helpers, and contract tests covering 200/404/409 behavior.
</objective>

<execution_context>
@/Users/shalom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shalom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-results-api-access/06-RESEARCH.md
@.planning/phases/02-job-intake-and-visibility/02-job-intake-and-visibility-02-SUMMARY.md
@.planning/phases/05-reviews-collection-controls/05-reviews-collection-controls-02-SUMMARY.md
@src/api/routes/job-status.ts
@src/storage/places-repo.ts
@src/storage/place-reviews-repo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add job-scoped ordered review read support for results assembly</name>
  <files>src/storage/place-reviews-repo.ts, src/storage/place-reviews-repo.test.ts</files>
  <action>Extend the review repository with a `listByJob(jobId)` read method that returns deterministically ordered rows (`place_key`, `position`, `id`) for a completed job in one query. Keep existing `listByJobAndPlace` behavior intact for compatibility, and add focused repository tests proving ordering and job scoping are stable.</action>
  <verify>
    <automated>npm run test -- src/storage/place-reviews-repo.test.ts -x</automated>
    <manual>Optional: inspect a seeded job with multiple places and confirm reviews are returned grouped-friendly in deterministic order.</manual>
    <sampling_rate>run after repository method and tests are added</sampling_rate>
  </verify>
  <done>Results route can fetch all job reviews in one deterministic repository read without per-place query loops.</done>
</task>

<task type="auto">
  <name>Task 2: Implement state-gated GET /jobs/:id/results with structured read-model response</name>
  <files>src/api/routes/job-results.ts, src/api/routes/job-results.test.ts</files>
  <action>Create `registerJobResultsRoutes` exposing `GET /jobs/:id/results` with explicit contract behavior: return `404` for unknown jobs, `409` for queued/running/failed jobs, and `200` only for completed jobs. For completed jobs, read places and reviews from repositories, group reviews by `placeKey`, and return stable structured payload including job status metadata and per-place nested reviews. Define route response schemas for status-code-specific payloads; do not re-run crawler logic or write new persistence in this path.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-results.test.ts -x</automated>
    <manual>Optional: create queued/running/failed/completed jobs in a temp DB and confirm each state returns the expected status code and payload shape.</manual>
    <sampling_rate>run after route implementation and contract tests</sampling_rate>
  </verify>
  <done>`/jobs/:id/results` has clear, test-locked state behavior and returns structured completed output with nested ordered reviews.</done>
</task>

<task type="auto">
  <name>Task 3: Register results route in server runtime without regressing existing endpoints</name>
  <files>src/server.ts, src/api/routes/job-results.test.ts</files>
  <action>Wire the new results route into server bootstrap using existing repository instances so runtime exposes both `/jobs/:id` (status) and `/jobs/:id/results` (output). Keep worker lifecycle and existing route registration order reliable, and extend route tests as needed to verify endpoint availability in the assembled app context.</action>
  <verify>
    <automated>npm run test -- src/api/routes/job-results.test.ts src/api/routes/job-status.test.ts -x</automated>
    <manual>Optional: run the API locally and call both endpoints for the same job to validate status polling and results retrieval remain complementary.</manual>
    <sampling_rate>run after server registration changes</sampling_rate>
  </verify>
  <done>Server exposes the new results API alongside existing job routes with no regression to job-status behavior.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/storage/place-reviews-repo.test.ts src/api/routes/job-results.test.ts src/api/routes/job-status.test.ts -x` passes.
- `GET /jobs/:id/results` returns `200` only for completed jobs and never triggers crawl/extraction work.
- Completed response contains deterministic place ordering and nested review ordering per place.
</verification>

<success_criteria>
- OUTP-01 is satisfied through a dedicated API endpoint that returns structured persisted results for completed jobs.
- API behavior for unknown and non-completed jobs is explicit and test-locked (`404`/`409`).
- Results payload is stable, deterministic, and ready for Phase 7 JSON/CSV export surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/06-results-api-access/06-results-api-access-01-SUMMARY.md`
</output>
